appId: ${APP_ID}
---
- launchApp:
    clearState: true
- openLink: ${EXPO_URL}
  when:
    not:
      equal: ${EXPO_URL} ""
- assertVisible:
    text: "Welcome"
    timeout: 30000 # Expo Go takes longer to bundle and load

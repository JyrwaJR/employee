# Memory Log

<!-- Append log entries here -->

- **2026-06-16 15:56 [AGENT-SPAWN]**
  - **Role:** Bug Fixer
  - **Scope:** Fix "text must be render in a text component" error in LoginScreen.tsx
  - **Parent Task:** Fix text component issue in LoginScreen.tsx
  - **Created Files:**
    - `.agents/rules/common/security.md` [FILE-CREATED]
    - `.agents/rules/common/coding_standards.md` [FILE-CREATED]
  - **Actions:**
    - Replaced `{__DEV__ && ...}` with `{__DEV__ ? ... : null}` in `src/features/auth/screens/LoginScreen.tsx`.
    - Validated changes with `npx tsc --noEmit` and `npx eslint`.
  - **Status:** COMPLETE

- **2026-06-16 16:00 [AGENT-SPAWN]**
  - **Role:** ESLint Configurator
  - **Parent Task:** Configure ESLint for TanStack Query and Unused Imports
  - **Scope:** Install @tanstack/eslint-plugin-query, update eslint.config.js, and verify clean lint execution.
  - **Created Files:**
    - `.agents/rules/frontend/index.md` [FILE-CREATED]
    - `.agents/skills/planner.md` [FILE-CREATED]
    - `.agents/skills/architect.md` [FILE-CREATED]
    - `.agents/skills/tdd-guide.md` [FILE-CREATED]
    - `.agents/skills/security-reviewer.md` [FILE-CREATED]
  - **Actions:**
    - Installed `@tanstack/eslint-plugin-query` via `pnpm add -D`.
    - Integrated `@tanstack/eslint-plugin-query` flat configuration into `eslint.config.js`.
    - Removed unused imports `Switch` from `ProfileScreen.tsx` and `useTheme` from `SettingsScreen.tsx`.
    - Added `empId` dependency to `queryKeys.pension.list` and `PensionScreen.tsx` useQuery key.
    - Destructured query results explicitly in `usePayslipData.ts` to avoid object rest destructuring warning.
    - Fixed pre-existing syntax / import errors in `settings/index.tsx` and `home/index.ts`.
    - Validated clean lint execution via `pnpm run lint`.
  - **Artifacts:**
    - `security_review.md` (in artifact directory)
  - **Security Findings:** none
  - **Status:** COMPLETE

- **2026-06-16 16:04 [AGENT-SPAWN]**
  - **Role:** Prettier Configurator
  - **Parent Task:** Configure Prettier
  - **Scope:** Create .prettierrc, remove prettier.config.js, and verify format execution.
  - **Actions:**
    - Created `.prettierrc` JSON file with the equivalent settings from `prettier.config.js`.
    - Deleted the redundant `prettier.config.js`.
    - Executed `pnpm run format` and `pnpm run lint` to confirm everything is valid and verified.
  - **Status:** COMPLETE

- **2026-06-16 16:15 [AGENT-SPAWN]**
  - **Role:** Naming Standards Compliance Engineer
  - **Parent Task:** Endpoint Naming Standardization & Type Fixes
  - **Scope:** Standardize endpoint filenames and variables, convert dynamic parameter endpoints to functions, and resolve blocking compilation errors.
  - **Actions:**
    - Renamed all `*.endpoints.ts` to `*.endpoint.ts` in all feature directories.
    - Standardized exported endpoint constants, registry, and all nested property/children keys to SCREAMING_SNAKE_CASE (e.g., `AUTH_ENDPOINT.ME`, `ENDPOINTS.EMPLOYEE.LIST`) to follow the project's literal constant rules.
    - Restructured `src/shared` directories to strictly comply with naming conventions:
      - Renamed `src/shared/store` to `src/shared/stores` (plural).
      - Merged/Moved files from `src/shared/context/` into `src/shared/providers/` and deleted the redundant `context` folder.
      - Merged/Moved `ENDPOINTS` registry and `queryKeys` from `src/shared/api/` into `src/shared/constants/` and deleted the redundant `api` folder.
    - Updated index exports in feature `index.ts` files.
    - Updated `src/shared/constants/endpoints.ts` to use singular key `EMPLOYEE`, import singular files, and export `ENDPOINTS`.
    - Converted dynamic parameter routes to functions instead of strings (fixing compilation errors).
    - Fixed TypeScript errors in `AuthFooter.tsx`, `CustomTabBar.tsx`, and `FadeInView.tsx`.
    - Cleaned up old endpoints files with `rm`.
    - Verified compilation with `npx tsc --noEmit` and linter execution with `pnpm run lint`.
  - **Status:** COMPLETE

- **2026-06-16 16:37 [AGENT-SPAWN]**
  - **Role:** Refactoring & Directory Architect
  - **Parent Task:** Remove notify/notifications and standardize barrel exports
  - **Scope:** Remove notify.ts, notifications.ts, update all usages to direct toast, and create/update barrel exports in all shared/components directories.
  - **Actions:**
    - Replaced the over-engineered `notify()` utility with direct `toast` calls (using `toast.success` and `toast.error`) across 12 files in auth, employee, salary features and shared hooks/providers.
    - Updated `src/shared/types/api.ts` to clean up the comments referencing the deleted notify utility.
    - Deleted `src/shared/utils/notify.ts` and `src/shared/constants/notifications.ts` from the codebase.
    - Created missing barrel exports (`index.ts` / `index.tsx`) in all subdirectories under `src/shared` including:
      - `src/shared/components/` (and subdirectories: `auth`, `base`, `display`, `feedback`, `screens`, `layout/Tabs`)
      - `src/shared/config/`
      - `src/shared/hooks/`
      - `src/shared/providers/`
      - `src/shared/services/`
      - `src/shared/stores/`
      - `src/shared/types/`
      - `src/shared/validators/`
      - `src/shared/utils/` (and subdirectory `helper`)
    - Standardized `TabLayout.tsx` to use named exports as preferred by the coding guidelines.
    - Verified compile safety with `npx tsc --noEmit` and linter checks with `pnpm run lint`.
    - Archived active feature plan under `plans/archive/remove_notifications_and_barrel_exports/`.
  - **Status:** COMPLETE

